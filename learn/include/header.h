#ifndef HEADER_H
#define HEADER_H

#include <unistd.h>
#include <string.h>
#include <stdio.h>

void reverse_count(int n);
int sum_array(int *n, int size);
int count_even_numbers(int *n, int size);

#endif